# 文件头规则

这个文件要求项目中的每个文件在开头说明自己的作用和功能。

- Python 文件必须使用模块文档字符串说明用途
- 测试文件必须在开头说明覆盖范围
- benchmark 文件必须在开头说明测试对象和当前阶段
- Markdown 文件必须在标题下第一段说明用途
- TOML 文件必须在开头用注释说明用途
- JSON 文件必须在开头使用 `$comment` 这类可读字段说明用途
- 修改旧文件时，同步补齐缺失的文件头说明