---
paths:
  - "tests/**/*.py"
  - "mini_infer/**/*.py"
  - "benchmarks/**/*.py"
---

# 验证规则

这个文件约束代码、测试和 benchmark 修改后的最小验证要求。

- 修改 Python 代码后，优先运行最小相关测试，而不是一上来跑全量
- 如果没有 Python 环境、依赖或 GPU 条件，明确列出无法验证的原因
- 对推理逻辑的修改，至少说明：功能验证方式、潜在回归点、后续需要的 GPU 实机验证
- 没有验证证据时，不要使用“已完成且可用”这类表述
