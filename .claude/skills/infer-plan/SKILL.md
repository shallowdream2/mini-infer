---
name: infer-plan
description: 为 mini-infer 规划阶段任务、范围和验收标准。
argument-hint: [任务或阶段]
---

# infer-plan

这个文件用于为当前任务输出阶段计划、执行顺序和验收标准。

把附加文本视为当前任务描述，并按以下结构回答：

- 目标
- 范围
- 非目标
- 关键文件
- 执行顺序
- 验收标准
- 风险与回避方案

默认不改代码，除非用户明确要求直接实现。
