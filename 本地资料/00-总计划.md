# 总计划

这个文件用于记录项目总目标、阶段目标和当前下一步，是本地长期计划的总入口。

## 项目题目

基于 PagedAttention 的高性能大模型推理引擎

## 当前状态

当前公开仓库仍处于初级骨架阶段，正式实现尚未开始。

## 当前环境前提

- 当前默认已经位于 Ubuntu 24.04 项目环境内
- 真实运行和 benchmark 目标环境是 2 × RTX 4090
- 没有 Ubuntu GPU 实测时，不给性能结论

## 最终目标

在单机双 `RTX 4090` 环境下，面向 `Qwen2.5` 这一类 `decoder-only` 大模型，实现一个基于 `PagedAttention` 的高性能推理引擎。该引擎支持块化 `KV Cache`、`Block Table` 映射、`Prefill / Decode` 分离、`Continuous Batching` 和流式生成，并在真实 benchmark 中相较 `HuggingFace Transformers` baseline 展现更好的吞吐、显存利用率和并发承载能力。

## 必须做成的能力

- 真实可跑的 `LLMEngine.generate()`
- `Paged KV Cache`
- `Block Table`
- `Prefill / Decode` 两条执行路径
- `Continuous Batching`
- 基准测试与结果分析

## 阶段目标

### 第一阶段

- 单卡跑通真实模型
- 完成最小 baseline benchmark

### 第二阶段

- 实现分页 KV cache 和调度器
- 建立完整指标体系

### 第三阶段

- 双卡扩展与性能分析

## 当前下一步

- 把当前桩实现替换成 HuggingFace 单卡真实推理链路
- 补齐 baseline benchmark
- 保持代码结构不扩张，只做核心主链路
